{
    "installCommand": "npm install && chmod +x node_modules/.bin/react-scripts",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}